```clojure
(doc map)
```

```clojure
(doc clojure.core/map)
```

```clojure
(apropos "map")
```

```clojure
(clojure.repl/doc map)
```

```clojure
(meta map)
```

```clojure
(require '[clojure.pprint :refer [pprint]])
(pprint map)
```

